# P4–P6 Acceptance Evidence

**Branch:** `feature/astock-research-phase1`  
**Date:** 2026-07-21  
**Policy:** Phase N gate green before Phase N+1 coding; no PR required.

## Gate results (re-run)

| Phase | Command | Result |
|-------|---------|--------|
| P4 | `pytest tests/apps/astock/test_phase4_gate.py tests/apps/astock/test_phase4_api.py -q` | **9 passed** |
| P5 | `pytest tests/apps/astock/test_phase5_gate.py -q` | **12 passed** |
| P6 | `pytest tests/apps/astock/test_phase6_gate.py -q` | **10 passed** |
| Full | `pytest tests/apps/astock/ -q` | **279 passed** |
| Manifest | `pytest tests/apps/astock/test_p4_p6_acceptance_manifest.py -q` | included in full |

## Boards (accepted)

- `docs/Phase4_任务平台_工单板.md` — Status: **accepted**; P4.E `[x]`
- `docs/Phase5_研究评价中心_工单板.md` — Status: **accepted**
- `docs/Phase6_自动寻优与持续运行_工单板.md` — Status: **accepted**

## Commits (pushed)

- `ee0cb78` feat(astock): phase-4 durable research queue, trials, workers, API
- `91c3951` feat(astock): phase-5 evaluation center scoring, OOS, pareto, gua-gain
- `6a4bed4` feat(astock): phase-6 budgeted search, schedules, drift, auto summary
- `f7b7d92` fix(astock): phase-4 board checklist consistency and phase-5 regime gates
- `fb18a25` test(astock): add P4-P6 acceptance manifest for boards and surfaces

## Live stack note

Redis/Celery/PostgreSQL/Optuna not installed in this environment. Phase 4 gate uses SQLite durable queue (`research_platform.db`) + Memory/SQLite fakes and FastAPI TestClient. Core outcomes proven: enqueue, atomic claim, idempotent trials, cancel, pause/resume, nack/retry, reclaim_stale, restart durability.

## Acceptance criteria map

1. **P4** — Durable tasks/trials leave Web-only path via `ResearchPlatform` + SQLite queue; workers claim/ack; reclaim after stale heartbeat; pause/cancel/retry via API `/api/v1/research/*`.
2. **P5** — `evaluate_trials` / scoring / walk-forward / regimes / gua-gain / heatmap / Pareto; ranking not total_return-only (`test_phase5_gate` + manifest).
3. **P6** — Budgeted grid/random/staged search, fixed seed, full-retest selection, daily/nightly/weekend schedules, drift, paper-candidate mark, APIs under `/api/v1/research/{search,schedules,drift,summary}`.
4. **Sequential gates** — P4 green then P5 then P6; boards accepted after each.
5. **Full suite + notify** — 279 passed; branch pushed; user notified (no PR).