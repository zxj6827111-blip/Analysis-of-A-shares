# internal/tushare_factor_qfq 质量与对比验收报告

| 项目 | 值 |
|---|---|
| 日期 | 2026-07-26 |
| 判定 | **PASS** |
| 对象 | `internal_tsfqfq_1d_20260717_c962acb8af26`(5554 只 / 15,562,445 行 / 20010427~20260717) |
| 证据 | tmp/_gc_validate.json、tmp/tushare_factor_quality_issues.csv、tmp/tushare_factor_qfq_comparison.csv、tmp/_gc_incremental_rehearsal.json |

## A. 全量结构与数值验收

1. **结构**:5554/5554 逐只校验——trade_date 严格升序且无重复(0 违例)、OHLC>0(0 违例)、manifest 引用 blob 全存在、**5554 个 blob SHA256 逐一重算一致**、行数重算=manifest。
2. **数值独立复核**:随机 300 只×随机日,由因子 blob 独立重算 `raw×asof/anchor` 与库内值比对——**300/300 完全一致(≤5e-5)**。
3. **anchor 恒等式**(设计不变量:cutoff 锚点使末段 ratio=1 ⇒ 派生价≡raw 价):5548 只成立;6 只偏离经逐只溯源**全部为合法语义**——其末次交易日之后、cutoff 之前仍有因子事件(退市/停牌期间公司行为:920680、000004 国华退、002808 恒久退、002898 赛隆退、300029、300379),锚定 cutoff 因子正确反映该事件。
4. **raw 源异常继承**:local_vendor 已知 22 只/25 行 OHLC 边界问题中,进入派生宇宙的 2 只如实继承并在 quality_issues.csv 标注 `raw_source_inherited`(其余 20 只为无因子北交所股,不在派生集)——未静默修正、未误判为因子问题。
5. **因子层校验**:store_factors 入库即拒绝乱序/重复/≤0;同步层重复因子日期 keep-last 去重并计数;全量 0 quality_failed。

## B. 与 Tushare 原生 qfq(ts.pro_bar adj="qfq")抽样对比

100 只分层(五板块+长史+新股+最新年缺席+Tushare 可得退市股+多次公司行为),29,056 日 / 116,224 单元:

| 统计 | 值 | 解释 |
|---|---|---|
| 2dp 完全一致率 | **99.55%** | 主对比口径(Tushare 输出 2dp) |
| 4dp 完全一致率 | 18.7% | 预期低——精度口径差(我方 4dp) |
| 最大绝对误差 | 0.005 | 恰为 2dp 舍入半步 |
| 最大相对误差 | 3.46% | 低价股上的同一舍入差放大 |
| 锚点不匹配 | 0 只 | end_date=cutoff 对齐后无系统性比例偏移 |
| 公司行为日附近 | 无特异差异 | Top50 差异全为舍入级 |
| 北交所 18 只 / 退市 8 只 | 同上 | 无分层特异 |
| 日期覆盖 | 派生侧 0 多余;Tushare 侧多 20,681 天 | =前导缺口 + 北交所新三板时期行情(vendor 无),分类记录 |

**未为提高一致率修改任何 raw 价格。** 差异根因分类:①输出精度舍入(主体);②数据源日期覆盖(前导缺口/新三板期);无第三类。

## C. 增量与修订(factor_inc_v1 演练)

历史因子修订→受影响股全历史重算(day1 价格变化,手算核验);新公司行为→anchor 变化→整段重算;同 raw 不同 factor→dataset_id 必变;旧 dataset/旧回测绑定不受影响;39 项新单测含"最新因子变化/历史修订/新公司行为/缓存不复用旧信号"。

## 结论

派生数据集在结构、数值、血缘、对比、修订五个维度全部通过;两处已知非阻断事项(前导缺口 49 只丢行、raw 继承边界 2 只)均有逐笔审计记录。
