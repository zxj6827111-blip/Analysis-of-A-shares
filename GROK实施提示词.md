# 给 Grok 的实施提示词

你现在位于 Windows 项目：

`E:\Software Development\wtpy-master`

你的任务是严格依据根目录计划文档完成代码落地：

`E:\Software Development\wtpy-master\A股多周期TN6指标与八卦回测实施计划.md`

请先完整阅读该计划，再检查真实代码、配置、示例和本地数据结构。不要只根据 README 推断项目能力，也不要重新发明或擅自改变已经锁定的业务规则。

## 必须完成的目标

1. 在 `wtpy/apps/astock/` 建立独立 A 股扩展，实现通达信日线数据导入、数据质量检查、沪深 A 股股票池、交易日历、复权、多周期聚合和运行配置。
2. 建立统一指标注册中心，使 `指标` 目录中的 4 个 `.tn6` 成为第一批默认可见指标，并正确处理 SHA256 去重、别名、版本和状态。
3. 按“稳定混合导入”实现 TN6：系统可以接收和登记 `.tn6`，执行必须依赖显式配对的公式源码；禁止逆向破解、猜测或伪造 `.tn6` 内容。
4. 实现安全的通达信公式 tokenizer、parser、AST、编译器和运行时，不得使用 Python `eval`；覆盖现有文本公式需要的语法和函数。
5. 实现日线、周线、月线独立计算和 V5 三周期共振，严格避免未来数据。
6. 将 Excel 384 爻内容规范化为可追溯知识库，实现八卦 OHLC 算法，并将 `bagua_ohlc` 注册为与通达信指标并列的可选指标。
7. 实现指标研究、八卦条件统计、WonderTrader 组合回测、CSV/Excel 报告和计划中列出的 CLI。
8. 增加最小但完整的单元测试、集成测试和黄金样本验证。

## 不得违反的边界

- `D:\通达信` 只能读取，禁止修改、移动或删除任何原始文件。
- 第一版只处理沪深 A 股，不加入北交所。
- 不得把 `MIN60` 偷换成周线或月线；分钟数据不足时必须清楚标记并禁用正式回测。
- 不得把八卦古文主观转成买卖信号。八卦单独选择时只运行分类和条件收益研究；与技术指标同选时只附加卦象标签。
- 不得根据文件名自动认定 `.tn6` 和 `.txt` 等价，必须使用显式映射和哈希。
- `735金叉及趋势.tn6` 当前缺少源码。你必须实现并验证 `source_required` 流程，继续完成其他不依赖该源码的工作，不能虚构它的公式。
- 三个“双增多周期共振” `.tn6` 内容哈希相同，应当保留三个名称别名，但只建立一个内容版本。
- 不得覆盖或回退用户已有文件；修改前先检查工作区状态。
- 未经用户明确授权，不要执行 `git commit`、`git push`、merge、reset 或历史改写。

## 执行方式

按照计划中的阶段 A 至阶段 G 顺序推进。每个阶段必须做到：实现、运行对应验证、根据失败修正，然后再进入下一阶段。不要只创建空壳接口、TODO 或无法执行的占位代码。

如果 Python 3.9 x64 或某个外部依赖在本机不可用：

- 先完成不依赖该环境的代码和静态测试。
- 不要全局升级或破坏现有 Python 环境。
- 将准确命令、错误输出、影响范围和建议解决方式记录在交付报告中。

对于公式兼容性：

- 优先实现当前三个 `.txt` 公式实际使用的语法和函数。
- 明确处理序列预热、NaN、`C/CLOSE` 别名、`CROSS`、`BARSLAST`、`COUNT`、逻辑优先级和跨周期引用。
- 遇到未知函数必须报出指标、源码位置和函数名，禁止静默降级。
- 选取 10 至 20 只股票建立通达信黄金样本对比；没有黄金样本时，不得声称公式已与通达信完全一致。

对于八卦算法，必须通过固定用例：

```text
开盘 6.27 -> 6+2+7=15 -> 15%8=7 -> 上卦山/艮 ☶
收盘 5.90 -> 5+9+0=14 -> 14%8=6 -> 下卦水/坎 ☵
最高 7.33 -> 13，最低 5.90 -> 14
(13+14)%6=3 -> 第三爻六三
最终：䷃山水蒙；行情简判：诱多陷阱不可追涨，观望为上
```

除以 8 余 0 必须映射为 8=地/坤，除以 6 余 0 必须映射为第六爻。价格必须固定保留两位小数，不能因浮点误差丢失 `5.90` 的零。

## 验证要求

至少完成并记录以下验证：

- `.day` 解析首尾记录和异常文件测试。
- 周线、月线、节假日短周、停牌、新上市和未闭合周期测试。
- TN6 扫描、哈希去重、别名、源码配对和 `source_required` 测试。
- 通达信公式 parser、函数语义、错误诊断和无未来数据测试。
- Excel 64 卦乘 6 爻完整性和固定八卦样例测试。
- 指标单独选择、多个指标独立运行、显式 `all/any` 组合测试。
- 八卦单独研究和八卦附加技术指标信号测试。
- 小股票池端到端导入、信号、回测和报告测试。
- 能运行时，再执行 2016–2026 全市场；不能运行时如实说明阻塞，不得伪造结果。

## 最终交付格式

完成后在项目根目录生成：

`GROK_IMPLEMENTATION_REPORT.md`

报告必须包含：

1. 完成项与未完成项，逐条对应实施计划。
2. 新增和修改文件清单及各自职责。
3. 实际执行的安装、构建、测试和回测命令。
4. 测试数量、通过/失败/跳过情况和关键输出。
5. 小股票池回测的配置、指标、周期、日期范围和结果文件位置。
6. TN6 各文件的导入状态、内容哈希、源码映射和可回测状态。
7. 数据质量、幸存者偏差、复权、ST/涨跌停和分钟数据风险。
8. 需要用户补充的外部输入，尤其是 `735金叉及趋势` 源码。
9. 供下一位审核者复现的最短命令序列。

最终回复中同样给出简洁的结果、验证、风险和输出路径。不要只说“已经完成”，必须提供可独立复核的本地证据。
